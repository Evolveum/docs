= CSV Resource

A CSV resource represents a structured way to integrate external data stored in one or more CSV files into midPoint.
It allows tabular data to be treated as a managed resource, enabling synchronization, mapping, and processing within the identity management framework.

See how to create:

* xref:/midpoint/reference/admin-gui/resource-wizard/create-resource-using-wizard/[A simple CSV resource]
* xref:/connectors/resources/csv/multiple-csv-resource/[A CSV resource with multiple CSV files]
