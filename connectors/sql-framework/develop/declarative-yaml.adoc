= Declarative YAML
:page-nav-title: Declarative YAML
:page-display-order: 1240
:page-upkeep-status: orange
:page-toc: top
:page-since: 4.11
:page-description: Declarative YAML support in the SQL framework: YAML connector manifest and native YAML schema documents as an alternative to Groovy scripts.
:page-keywords: YAML manifest, declarative YAML, native schema, connector.manifest.yaml, no-code connector

The SQL framework supports a declarative YAML form for the connector manifest and for schema definitions, in addition to Groovy scripts.

include::link-to-intro-preamble.adoc[]

NOTE: YAML support covers a subset of the xref:/connectors/sql-framework/develop/schema-customization/[Groovy DSL configuration].
Use Groovy scripts for anything beyond simple table/attribute mapping (e.g., operation handlers, custom search).

// TODO: When first reading this, it wasn't immediately clear to me the two articles describe the same and only that YML offers less options.
//       That might have been my fault but it spurred an idea - if it wouldn't be clearer to create a section "Declarative schema customization" in which two articles would be, i.e., Groovy and YAML.
//       It is not as trivial as it sounds so I'm not implementing it right away—only if you wish to go this way.
//       Also, I have a feeling this article more or less requires you have read the Groovy customizations first, it feels less explanatory (at least on the first read).

== YAML connector manifest

The connector manifest may be named `connector.manifest.json`, `connector.manifest.yaml` or `connector.manifest.yml` — exactly one of the three may be present on the classpath.
// TODO: classpath -- link to explanatory details? (Similar to as we link add link on PATH)

[source,yaml]
----
connector:
  schema: <1>
    - script: /schema/customize.groovy
    - script: /schema/persons.native.schema.yaml
  operation: <2>
    - script: /handlers/user_ops.groovy
----
<1> List of schema script resources, loaded during initialization. Groovy and YAML schema documents can be mixed.
<2> List of operation handler script resources (Groovy only at present).

Paths starting with `/` are resolved as classpath resources from the root of the classpath.

== Native YAML schema documents

A schema document named `*.native.schema.yaml` (or `*.connid.schema.yaml`) is a flat YAML description of object classes:
// TODO: Not an issue here (except a link to explanation would be nice), but rather a topic for discussion:
//       I realize this is probably trivial for any reasonably experience developer, but do we have anywhere any explanation as to what's the difference between the two (native vs. ConnId)?
//       As far as I understand, it's the native schema of the target system vs the schema in the ConnId world.
//       But I'm not sure it is reasonably easy to infer from articles like /connectors/connid/, /midpoint/reference/resources/resource-schema/, or /midpoint/reference/resources/resource-schema/explanation/

[source,yaml]
----
# /schema/person.native.schema.yaml
objectClass: Person
sql:
  table: app_user
attributes:
  user_id:
    connId:
      name: __UID__
  user_name:
    connId:
      name: __NAME__
  user_email:
    connId:
      name: emailAddress
  loginCount: {}
----

Structure:

[cols="1,3", options="header"]
|===
|Key |Description

|`objectClass`
|ConnId object class name.

|`sql`
|Mapping block with `table` (SQL table name) and optional `schema` (SQL schema qualifier).

|`attributes`
|Map of `column name → attribute definition`.
    The only supported sub-block is `connId` with `name` (the ConnId attribute name, including `+++__UID__+++` / `+++__NAME__+++`).
    An empty value (`{}`) declares the attribute without renaming.
|===

YAML schema documents bind directly into the same live schema builder as Groovy scripts, so they merge with auto-discovered columns exactly like xref:/connectors/sql-framework/develop/schema-customization/[Groovy customizations] do — including correlation with discovery and the built-in mapping rules.
// TODO: So both Groovy and YAML customizations can be used for the same table (object class)? Or where is the limit?

== Groovy to YAML fallback

If a manifest entry references a `.groovy` resource that is not bundled, the loader transparently falls back to a `.yaml`/`.yml` resource with the same base name:

[source,yaml]
----
connector:
  schema:
    - script: /schema/users.groovy   # resolves to /schema/users.yaml when only the YAML file is bundled
----

This makes it possible to migrate a script to YAML incrementally without changing the manifest.
// TODO: Similar as to the question above - how granularly I can combine Groovy and YAML? (I.e., again, where are the combination limits)