== Java prerequisites

MidPoint is a Java application; therefore, it requires a Java platform to run on.

include::../../../java-versions.adoc[]

The `JAVA_HOME` environment variable should point to the Java installation path.